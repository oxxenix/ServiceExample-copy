ServiceExample chart has been successfully deployed!

You can access the Swagger UI locally with:

  kubectl port-forward svc/{{ include "serviceexample.fullname" . }} 9080:9080

Then open your browser at:
  http://localhost:9080/swagger

To verify pod status:
  kubectl get pods -l app.kubernetes.io/instance={{ .Release.Name }}
